import ListItem from "./ListItem";

const ListContent = (props) => {
    return (
        <div>
            <ListItem />
            <ListItem />
            <ListItem />
            <ListItem />
        </div>
    );
};

export default ListContent;
